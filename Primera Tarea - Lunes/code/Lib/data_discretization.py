import numpy as np
import pandas as pd

def data_discretization(df_clean, cancer_interes):
    # Utiliza esta función para discretizar las columnas

    # Ideas: usar np.select(condlist, choiceList), np.where()

    # Importante, el df que salga de aquí tiene que estar listo para el aprendizaje!


    return df_clean