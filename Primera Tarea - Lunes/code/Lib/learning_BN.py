import os.path
import pyAgrum as gum
from pyAgrum.lib.bn2graph import BN2dot 
import pyAgrum.lib.image as gumimage


def learning_BN(df_model, constraints, path):

    # En esta función será necesario especificar elementos como el algoritmo de aprendizaje, el prior, el score,
    # los arcos previos al aprendizaje ....

    learner = ''

    arcs = ''

    for (outward, inward) in arcs:
        ''

    model = ''


    return model